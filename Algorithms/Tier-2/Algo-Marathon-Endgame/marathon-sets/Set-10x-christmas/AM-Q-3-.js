/*################################################
PROBLEM 3:
Status: 
Q. Given an unsorted linked list with unique values, insert an element after the element at the target index.

If the target index exceeds the length of the given array, do nothing.
The index follows a zero based numbering rule (i.e. first element's index of the array is 0).
Example:

Given a linked list: 3 -> -1 -> 2 -> 5, element (to be inserted): 0, target: 2
// returns 3 -> -1 -> 2 -> 0 -> 5

##################################################*/

 
/* APPROACH
 

*/

// Function:
// Singly-linked lists are already defined with this interface:
// function ListNode(x) {
//   this.value = x;
//   this.next = null;
// }
//
function solution(head, target, value) {

}
