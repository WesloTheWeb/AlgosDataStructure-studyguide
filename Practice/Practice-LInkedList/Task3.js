/*
Q. Given a target integer and count integer, create a linked list of length count,
where each node has the value target.
*/

// Singly-linked lists are already defined with this interface:
// function ListNode(x) {
//   this.value = x;
//   this.next = null;
// }
//
function solution(target, count) {

}
