/*
Q. Given an array of integers, return an array of sum by adding two elements from each end. For odd length arrays, the last sum should be the center element by itself.

Example:
Input: [1, 2, 3, 4, 7]
Output: [8, 6, 3] // [1+7, 2+4, 3]
*/

function solution(array) {

}
