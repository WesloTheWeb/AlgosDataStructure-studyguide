/*
Q. Given an array of integers, flip an array.

Example:
Input: [1, 2, 3, 4, 5, 6]
Output: [6, 5, 4, 3, 2, 1]
[execution time limit] 4 seconds (js)

[input] array.integer array

[output] array.integer
*/

function solution(array) {
    return array.reverse();
}

// What is the proper alternative way to do it aside from just cheesing it with reverse();