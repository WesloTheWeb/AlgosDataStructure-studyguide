// ? Utility Functions
/*
function tree(x) {
    this.value = x;
    this.left = null;
    this.right = null;
};

function ListNode(x) {
  this.value = x;
  this.next = null;
};

*/

/*
ARRAY
Question 1:
Given a non-empty array of inegers, return the maximum sum by adding up all of the integers in a non-empty subarray of the
input array.
    - A subarray must only contain adjacent numbers.

Examples:
1.) Gvien an array: [-2, 1] returns 1
2.) Given an array: [3, 4, -6, 7, 9] returns 17 
(3 + 4 +(-6) + 7 + 9)
*/

function maximumSum(array) {

};

/*
ARRAY
Question 2:
Given a non-empty array of inegers, find its longest increasing subsequences.
    - A subsequence of an array is a set of numbers that aren't necessarily adjacent int he array but int he same order
    as they appear.

Examples:
1.) Gvien an array: [2, 3, -1] returns [2, 3]
2.) Given an array: [99, 1, 3, 4, 5, 100] returns [1, 3, 4, 5, 100]

*/

function longestSubsequence(array) {

};

/*
ARRAY
Question 3: [Three Sum]
Given an array of integers, find all unique triplets (a, b, c) in the array such that their sum equals zero. (a + b + c = 0).
    - For test purposes, you must return one sorted triplet with the smallest overall value.

Examples:
1.) Input: [1, 2, 0] 
returns []

2.) Input: [-1, 0, 1, 0, 1] 
returns [[-1, 0, 1]]

3.) Input: [-5, -1, 0, 1, 4, -1]
returns [[-5, 1, 4], [-1, 0, 1]]
*/

function threeSum(array) {

};

/*
LINKED LIST
Question 4: 
Given a linked list, return the value of the element that is at the 1/3 position from the head. 
You may assume the total number of elements is multiples of 3 (e.g: 3, 6, 9, 12 ...).

Examples:
1.) Input: 1 -> 2 -> 3 -> 4 -> 5 -> 6
returns 2

2.) Input: 1 -> 2 -> 3
returns 1

function ListNode(x) {
    this.value = x;
    this.next = null;
};
 
*/

function linkedListOneThird(array) {

};

/*
ARRAY
Question 5: 
Given a string, return the index of the first occurrence of a target string. Return -1, if the input string
does not contain the target string.

Examples:
1.) Input: 'hello', target: 'll'
returns: 2

2.) Input: 'formation', target: 'afor'
return: -1
*/

function firstOccurrence(string, target) {

};
